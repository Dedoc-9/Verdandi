{
 "name": "verdandi-preregistration",
 "version": 1,
 "reading": "a fork of executable-epistemics' registry: a rung that will produce a number on a host registers its hypothesis, success condition, failure condition and interpretation limits HERE, before its instrument runs; the entry's chain hash is what the rung's record must cite; an entry without a failure condition is refused a seat",
 "entries": {
  "RECORD-0": {
   "rung": "RECORD-0",
   "hypothesis": "every record Verðandi mints can be written by two languages and read through one firewall without a verdict entering data",
   "success_condition": "records-firewall PASSES over every committed record; records-twins shows Python recomputes the chain hash of every Rust-written record; a planted verdict key and a planted tampered hash are both refused by BOTH readers",
   "failure_condition": "any committed record lacks a field, carries a verdict-shaped key, or has a chain hash the other language cannot reproduce; or a plant passes either reader",
   "interpretation_limits": [
    "a chain hash detects tampering after sealing; it does not make the data true — `check` and the rows do that",
    "the firewall scans keys, not meaning: a verdict can still be smuggled as a value or a string, and is caught only by reading"
   ],
   "instrument": "verify/verify.py rows records-firewall, records-twins, records-preregistered",
   "chain_hash": "766615422be3f06d70163eec36aba7abad507cc121ed4a96b300897014d4d6a2"
  },
  "SHELL-0": {
   "rung": "SHELL-0",
   "hypothesis": "a hand-rolled Win32 shell (zero crates) can blit the kernel's composite into a window, prove it blitted what the kernel made, and time frame-ready -> composited on the owner's host",
   "success_condition": "the blit-hash law holds headless (a byte altered between kernel and blit reddens; the plant bites); on the host, N frames blitted with every blit hash equal to the kernel's composite sha, and a record of frame-ready -> composited p50/p95/p99/max µs with DwmGetCompositionTimingInfo's refresh period beside it, sealed under the envelope with the budgets as data",
   "failure_condition": "a blit whose hash differs from the kernel's composite goes unrefused; or no composited timestamp can be read from DWM on the host; or the record cannot be sealed without a verdict-shaped key; or the number is not reproducible in shape (order of magnitude) on a second run",
   "interpretation_limits": [
    "frame-ready -> composited is not input-to-photon: input transport, the present wait beyond composition and the panel need capture hardware",
    "a composed GDI present costs at least one refresh interval under DWM (documented); this number is a BASELINE for a later flip-model shell, not a claim of low latency",
    "Windows-only; a second shell is a later rung"
   ],
   "instrument": "shell/win32.rs + verify rows shell-blit-hash, shell-blit-plant; off-gate: shell --measure N on the host -> shell/attest/present-<host>.json",
   "chain_hash": "de30d1ec9bf60a0a44acd7ee7bd9de4ca9c87d78a3ee5dbc5ecd2cf25c08d8cd"
  },
  "LATENCY-0": {
   "rung": "LATENCY-0",
   "hypothesis": "for the fixed sealed playback sequence, the host's frame-ready -> composited present path fits one 144Hz frame period (SOFTWARE-144-BUDGET) AND the panel refreshes at >=144Hz (HARDWARE-144), each measured and reported independently; SUSTAINED-144Hz is their conjunction (A AND B), never the software budget alone",
   "success_condition": "on the host, replaying the sealed reference session through shell playback-window --measure, a record of per-frame frame-ready -> composited p50/p95/p99/max us with the measured panel refresh period beside it, sealed under the envelope; SOFTWARE-144-BUDGET is PASS iff p99 <= 6944 us; HARDWARE-144 is PASS iff the measured refresh period <= 6944 us (>=144 Hz); SUSTAINED-144Hz = SOFTWARE-144-BUDGET AND HARDWARE-144",
   "failure_condition": "p99(frame-ready -> composited) > 6944 us fails SOFTWARE-144-BUDGET; a measured refresh period > 6944 us fails HARDWARE-144; either alone refutes SUSTAINED-144Hz; also fails if no composited timestamp can be read from DWM on the host, the record carries a verdict-shaped key, or the shape (order of magnitude) is not reproducible on a second run",
   "interpretation_limits": [
    "SOFTWARE-144-BUDGET is a BUDGET condition: 99% of frame-ready -> composited intervals fit the nominal 144Hz period (6944 us). It is NOT by itself sustained-144Hz presentation and it is NOT a refresh-rate claim",
    "HARDWARE-144 is MEASURED from the DwmFlush interval, never inferred from the monitor's nominal setting",
    "frame-ready -> composited excludes the kernel render (that is the bench), input transport, the present wait beyond composition, and the panel scan-out; it is NOT input-to-photon (that needs capture hardware)",
    "the wait after composition is an instrument limitation of a composed GDI present; a flip-model / waitable-swapchain path is a later rung",
    "the sequence is the fixed sealed reference session, not arbitrary interactive load; Windows-only"
   ],
   "instrument": "shell/win32.rs playback_window --measure N (off-gate, host, --cfg shell_window) -> a raw record sealed by verify/seal_latency.py -> shell/attest/latency-<host>.json, citing this entry; verify row latency-preregistered locks this method",
   "chain_hash": "c4d8db6a3dd3293748d69629d1148801507725829d23fcab30e730824c1f56c7"
  },
  "GAUNTLET-0": {
   "rung": "GAUNTLET-0",
   "hypothesis": "the reference render (strips + frame + emit) decomposes at the kernel's pub-phase boundaries; the render phase with the largest p99 render-share, IF >= 500 permille, is GAUNTLET-1's optimization target; the witness hashes (frame_digest, pixel_sha) are a verification-only cost the interactive render never pays and are reported separately; the floor cast lives inside frame() and is separable only by GAUNTLET-1's sibling renderer",
   "success_condition": "a host breakdown record reports per-phase p50/p95/p99/max us and each render phase's p99 render-share in permille, witnesses checked against the frozen corpus BEFORE any number; GAUNTLET-1 is seated ONLY against a render phase whose p99 share >= 500 permille, and GAUNTLET-1 must prove byte-identical output against the frozen reference (a differential oracle: frame_digest AND pixel_sha equal over the corpus plus adversarial cameras) BEFORE any speed claim",
   "failure_condition": "no render phase reaches 500 permille (no single-phase optimization is justified; the 'incremental floor cast' hypothesis specifically dies unless frame() >= 500 permille); or the phase times do not reconcile with the render total; or the shape is not reproducible on a second run; or the breakdown rendered anything other than the frozen witnesses",
   "interpretation_limits": [
    "host-CPU-specific absolute times; the p99 render-share (permille) is the portable quantity, not the microseconds",
    "coarse pub-phase granularity: floor-vs-walls inside frame(), and any sub-emit split, need GAUNTLET-1's sibling renderer, which then carries the differential byte-identity oracle",
    "mantle.rs is NOT modified; GAUNTLET-0 only times the existing pub calls",
    "the witness hashes (frame_digest, pixel_sha) are verification cost, NOT interactive-render cost; the render total excludes them",
    "a breakdown is a target-finder, not a speed improvement; no optimization is claimed by GAUNTLET-0; not a claim about any other scene, tile set or resolution"
   ],
   "instrument": "kernel/main.rs --breakdown N (off-gate, host) via verify/gauntlet.py -> kernel/attest/breakdown-<host>.json, citing this entry; verify row gauntlet-preregistered locks this method and the 500-permille decision rule",
   "chain_hash": "a8122b4f8e35f1c61fc92ccdea428e5ed965fabf095b5234422606565478753e"
  },
  "GAUNTLET-1": {
   "rung": "GAUNTLET-1",
   "hypothesis": "emit is GAUNTLET-1's target (from GAUNTLET-0). A candidate emit is ACCEPTED as a renderer iff it is byte-identical to the frozen mantle.rs emit (pixel_sha) with an unchanged frame (frame_digest) over the corpus PLUS adversarial cameras; correctness is mandatory and independent of speed. A candidate is PROMOTED to the fast path iff it is accepted AND measurably faster on ONE consistent apparatus; identical-but-slower is a correctness pass and a performance fail; different pixels is not a renderer at any speed",
   "success_condition": "gauntlet1-equiv PASSES (fast_pixels == pixels and fast_frame == frame over every corpus and adversarial case); gauntlet1-region names the dominant divide-work region deterministically; and a promotion is claimed only with byte-identity AND a same-apparatus host speed measurement (witnesses checked first) against a stated threshold, sealed off-gate as GAUNTLET-1b citing this entry",
   "failure_condition": "any case where fast_equal != OK, or fast_pixels != pixels, or fast_frame != frame, refuses the candidate as a renderer regardless of speed; a promotion claimed without byte-identity, or without a same-apparatus speed measurement, or by comparing against GAUNTLET-0's instrumented render absolute; or mantle.rs is modified (the reference must stay frozen)",
   "interpretation_limits": [
    "mantle.rs is the frozen correctness oracle: the differential compares candidate -> frozen, never the reverse, and the harness is not the oracle; the candidate is guilty until its bytes agree",
    "correctness (byte-identity) and performance (speed) are two SEPARATE courts; byte-identity is mandatory and gate-enforced, speed is judged independently off-gate",
    "the region measure is divide-work (a source-derived proxy: ceiling 0, wall 1, floor 4 integer divisions per pixel), NOT a wall-clock; it names a target, it is not a speedup",
    "a GAUNTLET-1b speed number is host-specific and is NEVER compared to GAUNTLET-0's instrumented render absolute (different apparatus); it is a same-apparatus frozen-vs-candidate delta",
    "GAUNTLET-1a commits NO optimization technique; the seed fast.rs is an exact transcription that only proves the harness"
   ],
   "instrument": "kernel/main.rs --fast (gate rows gauntlet1-equiv, gauntlet1-region) for correctness + the deterministic region measure; the host speed number is GAUNTLET-1b's, off-gate, sealed and citing this entry; row gauntlet1-preregistered locks this method and the two-court promotion rule",
   "chain_hash": "96c427499affb6a32f990ec4a9e65c50b6f5c48637dd28373c63dd76ea322615"
  },
  "RE-BREAKDOWN-1": {
   "rung": "RE-BREAKDOWN-1",
   "hypothesis": "After GAUNTLET-1c the emit cost structure shifted (a 536x floor divide cut bought ~12% wall-clock), so the next step is to MEASURE, not optimize. emit's internal cost is attributed two ways: Court A, a DETERMINISTIC structural measure (gate-enforced, wall-clock-free) of what work exists; and Court B, a host ablation of fenced probes (addr -> lookup -> full) timed against emit. The probes are MEASUREMENT APPARATUS, NOT candidate renderers: a probe may be wrong by construction provided its wrongness is isolated from the certified path and its work is a demonstrable subset of that path (proven by probe VERIFY == emit, byte-identical).",
   "success_condition": "Court A passes on gate: emitbd_verify OK (the probe VERIFY path is byte-identical to emit and the frozen witness), write coverage is exactly once over every framebuffer pixel, and the memory reads are 3 per textured pixel; the working-set cardinality and the floor texel-stride locality are reported as data. Court B is a same-apparatus host ablation sealed off-gate as verdandi-emit-breakdown citing this entry, with the deltas read as incremental attribution.",
   "failure_condition": "probe VERIFY != emit (the apparatus is not a subset of the certified path); or the production emit or emit_collapse calls a probe (the fence is breached); or a Court B delta is interpreted as a pure hardware-resource cost rather than an incremental ablation delta; or any optimization is committed on this rung before the number (RE-BREAKDOWN-1 measures, it does not optimize); or a Court B number is compared to GAUNTLET-0's instrumented render absolute.",
   "interpretation_limits": [
    "RE-BREAKDOWN-1 MEASURES, it does not optimize: no candidate renderer is committed on this rung",
    "Court A and Court B are two separate courts; Court A is a source-derived proxy for time (deterministic, gate-enforced), Court B is the wall-clock (host, off-gate)",
    "the Court B deltas (LOOKUP-ADDR, FULL-LOOKUP) are incremental wall-clock attribution under controlled ablation, NOT pure hardware-resource measurements; cache state, branches, scheduling and register pressure make the subtraction non-additive; the experiment possesses no architectural counters",
    "the probes are measurement apparatus, not renderers: they never enter fast.rs's promotion chain, the production emit calls none of them, and a differing probe pixel is expected, not a fault",
    "a Court B number is host-specific and is NEVER compared to GAUNTLET-0's instrumented render absolute (different apparatus)",
    "the promotion rule is locked before any result: lookup increment or pathological locality -> LOCK a data-layout/locality court; address/DDA -> arithmetic path; full-write -> framebuffer path; results disagree or nothing dominates -> DEFER, do not optimize by intuition"
   ],
   "instrument": "kernel/main.rs --emit-structure (deterministic Court A; gate rows rebreakdown1-structure, rebreakdown1-fence) and --emit-breakdown N (Court A + the host ablation ladder); kernel/fast.rs::probe is the fenced apparatus (emit_probe<MODE>, black_box-anchored, VERIFY == emit); the host number is sealed by verify/rebreakdown1.py citing this entry; row rebreakdown1-preregistered locks this method and the promotion table.",
   "chain_hash": "66c3cb640876465c6efccce8c5c3edd1b5f789340489e51c3a441f27f158a3b3"
  },
  "LOCALITY-0": {
   "rung": "LOCALITY-0",
   "hypothesis": "RE-BREAKDOWN-1b named the tile fetch as emit's leading cost (clean ~4.4x over the map indirection). A cache-friendlier floor-tile EXECUTION FORMAT reduces that stall while producing byte-identical pixels. Two layouts compete, each a lossless bijection: BLOCKED (8x8 cache blocks, cheap shift/mask index) and MORTON (Z-order curve, portable scalar bit-interleave, a larger index tax). The winner is the one with the lower net p99 (memory saving Y minus its index-arithmetic tax X) on a process-isolated same-apparatus court; it LOCKs the data-layout court. Content provenance is isolated from execution format: the swizzle is a pure re-format, never a content edit.",
   "success_condition": "locality0-equiv PASSES: both layouts byte-identical to the frozen emit over corpus + adversarial cameras AND on a non-vacuous synthetic distinct-per-texel render (mandatory, gate-enforced); each swizzle is a lossless bijection (locality0-bijection, proven on distinct data); the canonical-order content hash is unmoved while the storage layout moves (locality0-provenance); and a layout is promoted only with byte-identity AND a lower process-isolated p99 than the DDA baseline (4734 us p99 on the host), sealed off-gate as LOCALITY-0 citing this entry, with the accepted single-thread emit sealed as the GAUNTLET-2 baseline.",
   "failure_condition": "any layout not byte-identical to the frozen emit (a regression) refuses it; a swizzle that moves the tile CONTENT (unswizzled hash changes) rather than only the format; a speed claim from separately-run (non-interleaved) invocations, or compared to GAUNTLET-0's instrumented render absolute; or a layout committed to the accepted path without byte-identity AND a same-apparatus win over the DDA baseline.",
   "interpretation_limits": [
    "the Epistemic-Invariance boundary: variants are monomorphized and hot-swapped at the binary boundary, process-isolated per invocation, and interleaved to cancel drift; instruction-level invariance up to address-generation is a structuring goal, NOT gate-provable — the gate proves OUTPUT byte-identity, and the record says so",
    "correctness (byte-identity) is mandatory and gate-enforced; speed is a SEPARATE host court, process-isolated, judged against the DDA baseline (4734 us) — never GAUNTLET-0's instrumented absolute",
    "the per-band wall-clock deltas are host measurements; the per-band within-cache-line locality is a deterministic source-cost proxy, not a wall-clock",
    "content provenance is isolated from execution format: the swizzle moves the layout, not the certified content (the text-reformat law applied to the tile)",
    "three exits: BLOCKED wins, MORTON wins, or NEITHER beats the baseline — the last is not a null but a proof the tile-fetch stall is a capacity/LRU miss no permutation fixes, redirecting to GAUNTLET-2 (parallelism) to hide the latency behind compute",
    "the accepted single-thread emit is sealed as the HARD BASELINE for GAUNTLET-2; the emit stays embarrassingly parallel (per-column wall, per-row floor, only the per-row DDA recurrence, independent across rows)"
   ],
   "instrument": "kernel/main.rs --locality (deterministic differential; gate rows locality0-equiv / -bijection / -provenance / -indextax) and --locality-bench --variant blocked|morton (process-isolated host timing, whole-frame + index tax X + per-band); kernel/fast.rs::locality is the fast-path apparatus (mantle.rs and fast::emit untouched); verify/locality0.py orchestrates interleaved invocations and seals verdandi-locality0 citing this entry; row locality0-preregistered locks the method, the three exits, and the process-isolation boundary.",
   "chain_hash": "28a9d792d5e0cb9443b804ae0bc75c12c2d5c0fcf2b96101c25011fe70f66a3a"
  },
  "GAUNTLET-2": {
   "rung": "GAUNTLET-2",
   "hypothesis": "LOCALITY-0 LOCKED the blocked floor-tile layout as the accepted single-thread emit and sealed its process-isolated p99 as the hard baseline. GAUNTLET-2 does NOT merely 'make emit multithreaded'; it establishes the stronger property that PARTITIONING THE COLUMN DOMAIN changes execution parallelism but not the certified picture. The law: emit over any partition of the columns into groups, processed in any order, across any admitted thread count T, is byte-identical to the frozen emit — thread count and column partition are EXECUTION parameters, not rendering authority. A candidate is ACCEPTED iff byte-invariant over every admitted (T, partition); it is PROMOTED iff its host p99 beats the sealed single-thread baseline at some admitted T greater than 1. The emit is embarrassingly parallel by construction, so parallelism hides the tile-fetch latency behind compute; it does not remove the capacity/LRU stall.",
   "success_condition": "TWO COURTS. Correctness (deterministic, gate-enforced): a partition-agnostic emit reproduces the frozen frame_digest AND pixel_sha for every tested column partition — contiguous chunks at T in {1,2,4,8,16}, AND adversarial partitions (strided/interleaved column assignment, reversed processing order, single-column groups, and a seeded permutation) — over the corpus plus adversarial cameras; the output bytes are invariant across all of them. Performance (host, off-gate): a threaded emit (std::thread, no crates) partitions the columns across an EXPLICIT thread count T that is an input to the benchmark and is recorded in the attestation (a T | correctness | p99 matrix), witnesses checked before any number; a promotion is claimed only with byte-identity AND a same-apparatus host p99 strictly below the sealed LOCALITY-0 single-thread baseline at some admitted T greater than 1, sealed off-gate citing this entry.",
   "failure_condition": "any (T, partition) where the picture differs from the frozen emit or from the T=1 contiguous result refuses the candidate regardless of speed — that is a partition or order dependency (shared framebuffer state, shared temporary state, mutable material-lookup state, column ordering, thread-local initialization, or reduction/merge ordering); a promotion claimed without byte-identity, without the explicit recorded thread count, without a same-apparatus measurement, or by comparing against GAUNTLET-0's instrumented render absolute or the old frozen renderer instead of the sealed single-thread baseline; or mantle.rs is modified; or the single-thread baseline is re-derived on this rung rather than inherited from the sealed LOCALITY-0 record.",
   "interpretation_limits": [
    "the single-thread baseline is INHERITED from LOCALITY-0's sealed accepted-emit p99 (the LOCKED blocked emit), never re-measured on this rung; GAUNTLET-2 must beat that exact hard floor, not a fresh number",
    "PARTITION INVARIANCE is the core property: the certified picture is byte-invariant under EVERY admitted column partition and processing order, not merely under thread count — thread count and partition are execution parameters, not rendering authority; adversarial partitions (strided/interleaved, reversed order, single-column groups, a seeded permutation) are tested, never merely contiguous chunks; thread-count invariance is the special case of contiguous partitions at T = 1, 2, 4, 8, 16",
    "byte-identity is mandatory and gate-enforced; correctness (the deterministic partition-invariance proof) and speed (the host threaded p99) are two separate courts, judged independently",
    "T is an EXPLICIT thread count, an input to the benchmark recorded in the attestation (the T | correctness | p99 matrix), never selected implicitly by the runtime",
    "the decision rule is preregistered BEFORE THE NUMBER: promote the parallel emit iff it is byte-invariant AND some admitted T greater than 1 has p99 below the sealed baseline; if p99 stops improving as T grows (sublinear or negative scaling), that is evidence of memory-bandwidth / cache contention, not insufficient parallelism, and the trajectory turns to the memory hierarchy rather than blindly increasing T",
    "a GAUNTLET-2 speed number is host-specific and is NEVER compared to GAUNTLET-0's instrumented render absolute, nor to the old frozen renderer, nor made into an absolute refresh-rate claim; it is a same-apparatus parallel-vs-sealed-single-thread delta",
    "the emit is embarrassingly parallel by construction (per-column wall, per-row floor, the per-row DDA recurrence independent across rows); parallelism HIDES the tile-fetch latency behind compute, it does not remove the capacity/LRU stall that LOCALITY-0's third exit named",
    "mantle.rs stays the frozen correctness oracle; the differential compares candidate -> frozen, never the reverse, and the harness is not the oracle"
   ],
   "instrument": "kernel/fast.rs will gain a partition-agnostic emit (deterministic correctness: byte-invariance over contiguous and adversarial column partitions; gate rows gauntlet2-*) and a std::thread parallel emit (host, off-gate, no crates); the host T | correctness | p99 matrix is sealed off-gate citing this entry; row gauntlet2-preregistered locks the partition-invariance law, the explicit-thread-count attestation and the decision rule (including the memory-hierarchy redirect on a scaling stall). Supersedes patch 0028's baseline-seal entry (5c3a5c30): no record cited it, and the instrument has not yet run.",
   "chain_hash": "711cc1d4982999c0e79aa72301bc7b4a0307e051517bef6deb7ad886d33b7b8a"
  },
  "LATENCY-1": {
   "rung": "LATENCY-1",
   "hypothesis": "GAUNTLET-2 LOCKED the T=8 threaded emit as the production render (~3.3x faster emit, byte-identical) and the shell now renders through it. LATENCY-1 asks — to be MEASURED, never assumed — whether replacing the renderer with that promoted T=8 path measurably changes the frame-ready -> composited latency of the fixed sealed playback sequence, versus the LATENCY-0 sealed baseline. It does NOT ask whether rendering is faster (that is already established); it asks whether the render headroom propagates into the measured presentation path or whether presentation/DWM remains the dominant measured boundary.",
   "success_condition": "on the host, replaying the SAME sealed reference session through the SAME apparatus as LATENCY-0 (shell playback-window --measure), but with the promoted T=8 threaded renderer, a record of per-frame frame-ready -> composited p50/p95/p99/max us sealed under the envelope with the measured refresh period beside it, witnesses checked first; the comparison is drawn ONLY against the LATENCY-0 sealed frame-ready -> composited baseline (the SAME observable), inherited from shell/attest/latency-<host>.json and never re-derived.",
   "failure_condition": "comparing the LATENCY-1 number against any observable other than LATENCY-0's sealed frame-ready -> composited baseline — in particular against an emit p99 (the GAUNTLET-2 7734 us baseline is an emit p99, a DIFFERENT observable, and using it is a category error); a claim of input-to-photon latency; no composited timestamp readable from DWM on the host; the shape not reproducible on a second run; the baseline re-derived rather than inherited from the sealed LATENCY-0 record; or the sealed session or the measurement apparatus changed from LATENCY-0's (then it is not the same experiment).",
   "interpretation_limits": [
    "the comparator is LATENCY-0's sealed frame-ready -> composited p99, NOT an emit p99: they are DIFFERENT observables (a render-phase time versus a present-path time) and must never be mixed; only the same-observable, same-apparatus delta is legitimate",
    "the single-thread render baseline is INHERITED from the sealed LATENCY-0 record (shell/attest/latency-<host>.json), never re-derived on this rung",
    "the disciplined reading of the delta: an IMPROVEMENT means the renderer headroom is propagating into the measured presentation path; NO MATERIAL improvement means presentation/DWM remains the dominant measured boundary; a DEGRADATION means the threaded path introduced a new scheduling/contention cost at the shell boundary; NONE of these by itself proves input-to-photon latency",
    "frame-ready -> composited excludes the kernel render (that is the bench), input transport, the present wait beyond composition, and the panel scan-out; it is NOT input-to-photon (that needs capture hardware)",
    "the promoted T=8 render's per-frame thread-spawn cost is UPSTREAM of frame-ready (it is part of producing the frame, not of presenting it), so it does not enter this observable directly; a render regression would show only insofar as it delays frame-ready",
    "Windows-only; off-gate on a named host; the sequence is the fixed sealed reference session, not arbitrary interactive load"
   ],
   "instrument": "shell/win32.rs playback_window --measure N (off-gate, host, --cfg shell_window), now rendering through the LOCKED fast::render (T=8) via present.rs; sealed by verify/latency1.py -> shell/attest/latency1-<host>.json, citing this entry and inheriting the LATENCY-0 baseline; row latency1-preregistered locks this method and the same-observable comparator.",
   "chain_hash": "8e93118e9a6f5447066b3b19e9c48c292fec2a96490189e1d924209879d1b4ee"
  }
 }
}
