# build outputs of the gate and the drivers
verify/build/
*.exe
*.pdb
*.o
# never committed
.env
__pycache__/
*.pyc
.gate-*/
# redirected gate / measurement output (sealed records under */attest/ are committed; raw logs are not)
*.log
# off-gate eyeball images from `kernel --write-png` etc. (the project commits digests, never images)
*.ppm
*.png
*.bmp
